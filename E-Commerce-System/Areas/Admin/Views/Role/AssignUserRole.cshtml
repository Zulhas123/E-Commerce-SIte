@*@model IEnumerable<E_Commerce_System.Areas.Admin.ViewModel.UserRoleMappingVM>*@
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}


<br />
<br />

<div class="row">
    <div class="col-6">
        <h2 class="text-info "> User Role Mapping  </h2>
    </div>

    @*<div class="col-6 ">
            <h2 class="text-right">
                <a asp-action="Create" class="btn btn-info"><i class="fas fa-plus"></i> &nbsp; New  Roles </a>
            </h2>


        </div>*@

</div>
<br />
<table class="table table-striped border">
    <thead>
        <tr class="table-info">
            <th>
                User Name
            </th>
            <th>
                Role Name
            </th>
          
        </tr>
    </thead>
    <tbody>

        @foreach (var item in ViewBag.UserRoles)
        {
            <tr>
                <td>@item.UserName</td>
                <td>@item.RoleName</td>

                @*<td style="width:150px">
                    <div class="btn-group">
                        <a asp-action="Edit" class="btn btn-primary" asp-route-id="@item.Id"><i class="fas fa-edit"></i></a>
                        <a asp-action="Details" class="btn btn-success" asp-route-id="@item.Id"><i class="fas fa-list"></i></a>
                        <a asp-action="Delete" class="btn btn-danger" asp-route-id="@item.Id"><i class="fas fa-trash-alt"></i></a>
                    </div>
                </td>*@
            </tr>
        }
    </tbody>


</table>

@section scripts{

    <script src="//cdn.jsdelivr.net/npm/alertifyjs@1.13.1/build/alertify.min.js"></script>

    <script type="text/javascript">



        $(function () {
            var Save = '@TempData["Save"]'
            if (Save != null) {
                alertify.success(Save);
            }

            var edit = '@TempData["edit"]'
            if (edit != null) {
                alertify.success(edit);
            }

            var del = '@TempData["Delete"]'
            if (del != null) {
                alertify.success(del);
            }
        })


    </script>
}